UALUnequipAndListen = UALUnequipAndListen or {}

UALUnequipAndListen.sandbox = RibsFramework.Sandbox:new({
    ID = "UALUnequipAndListen",
    instances = {
        classVersion = RadioTVCore.classVersion
    }
})
